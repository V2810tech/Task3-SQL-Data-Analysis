# Task3-SQL-Data-Analysis
Performed SQL-based data analysis on the Superstore dataset using MySQL. Demonstrated data extraction, filtering, sorting, grouping, and aggregation with SQL queries (SELECT, WHERE, ORDER BY, GROUP BY, COUNT, SUM, AVG) to generate meaningful business insights.
